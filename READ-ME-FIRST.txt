JONI'S COOKBOOK — BACKLINK FIX

This package contains ONLY the 12 current recipe HTML pages.

WHAT CHANGED:
Every recipe's top backlink now goes to:
    index.html#recipes

The visible link reads:
    ← Back to Recipes

That means clicking it returns directly to the "From Our Recipe Box" recipe list
on the new homepage instead of jumping all the way to the top.

UPLOAD:
1. Upload all 12 HTML files to the ROOT of joni-cookbook.
2. Replace the existing files with these same-named copies.
3. Do NOT replace index.html — the homepage is already correct.
4. Commit changes and wait for GitHub Pages to deploy.
5. Open any recipe, click "← Back to Recipes", and verify it lands at the recipe list.

No recipe content or Schema.org data was intentionally changed.
